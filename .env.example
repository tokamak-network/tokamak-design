# LiteLLM proxy (OpenAI-compatible) — used for the vision/LLM call.
# For Tokamak team, use the internal proxy:
LITELLM_BASE_URL=https://api.ai.tokamak.network/v1
LITELLM_API_KEY=
# Pick any model exposed by the proxy. Default is gemini-3.1-pro.
LITELLM_MODEL=gemini-3.1-pro
